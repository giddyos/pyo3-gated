[package]
name = "renamed-dep-user"
version = "0.0.0"
edition = "2024"
publish = false

[features]
default = []
python = ["dep:pyo3", "gated/python"]
stub-gen = ["python", "gated/stub-gen"]

[dependencies]
gated = { package = "pyo3-gated", path = "../../../crates/pyo3-gated" }
pyo3 = { workspace = true, optional = true }
