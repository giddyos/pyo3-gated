[package]
name = "python-user"
version = "0.0.0"
edition = "2024"
publish = false

[features]
default = []
python = ["dep:pyo3", "pyo3-gated/python"]

[dependencies]
pyo3-gated = { path = "../../../crates/pyo3-gated" }
pyo3 = { version = "0.28", optional = true }
