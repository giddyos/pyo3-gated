[package]
name = "renamed-facade-user"
version = "0.0.0"
edition = "2024"
publish = false

[features]
default = []
python = ["gated/python"]
stub-gen = ["python", "gated/stub-gen"]

[dependencies]
gated = { package = "pyo3-gated", path = "../../../crates/pyo3-gated" }
