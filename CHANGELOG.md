# Changelog

All notable changes to this project should be documented in this file.

## Unreleased

- Split normal Python support from stub generation.
- Added `py_compat`, `define_py_module!`, `pyfunction_args`, `pyo3_crate`, `#[rust_only]`, and top-level function `py_only` support.
- Added CI, packaging checks, and Python import smoke coverage.
